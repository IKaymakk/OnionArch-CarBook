@model CarBook.DTO.BlogDtos.BlogDetailDto

<div class="sidebar-box ftco-animate">
    <h3>Etiketler</h3>
    <div class="tagcloud">

    @foreach (var x in Model.TagClouds)
    {
                <a href="#" class="tag-cloud-link">@x.TagCloudTitle</a>
    }
    </div>
</div>